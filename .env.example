# Copy to .env.local (gitignored) and fill in real values.
# Both VITE_* keys are public, safe to ship in the client bundle.
VITE_SUPABASE_URL=https://rgqrbfgwhzoxavmheckj.supabase.co
VITE_SUPABASE_ANON_KEY=

# Web Push (browser → device notifications). Generate with `node scripts/gen-vapid.mjs`
# The PRIVATE key goes into Supabase Edge Function secrets, NOT here.
VITE_VAPID_PUBLIC_KEY=

# Debug only: set to '1' to force raw-fetch transport in lib/db.js.
# VITE_DB_USE_FETCH=1
